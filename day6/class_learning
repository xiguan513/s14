lass 类
    一个类即是一类拥有相同属性的的对象的抽象，在类中定义了这些对象都具备的属性，共同的方法

Object 对象
    一个对象即是一个类的实例化实例，一个类必须经过实例化后方可以调用，一个类可以实例化多个对象，每个对象可以有不同的属性

Encapsulation 封装
    在类中对数据的赋值，内部调用对外部用户是透明的

Inheritance 继承
    一个类可以派生出子类，父类里面定义的属性，方法自动被子类继承

Polymorphism 多态
    一种接口，多种实现

名词
    构造函数 在生成对象时调用，可以用来进行一些初始化操作，不需要显示去调用，系统会默认去执行。
            构造方法支持重载，如果用户自己没有重新定义构造方法，系统就自动执行默认的构造方法。__init__
    析构函数：实例释放，销毁的时候执行的，通常用于做一些收尾工作，比如关闭文件 __del__
    属性(变量)：
        类变量  (类属性)
        实例变量（静态属性）(实例属性)
        私有变量（私有属性）
        在普通方法的基础上添加 @property 装饰器，方法转属性（旧式类，和新式类不同的property装饰器方法）,此属性是实例属性
    方法：
        类对象方法"@classmethod"来进行修饰
        私有方法 def __name(self):
        动态方法 def name(self):
        静态方法"@staticmethod"来进行修饰

    对象：
        实例对象
            实例化是产生出一个类对象的实例，称作实例对象。
        类对象
            类对象支持两种操作：引用和实例化。引用操作是通过类对象去调用类中的属性或者方法，

    新式类，旧式类区别 多继承的顺序
    py2 旧式类按照深度优先集成，新式类按照广度优先来继承
    py3 新式类，旧式类按照广度优先来继承


    多态
    一种接口，多种实现
